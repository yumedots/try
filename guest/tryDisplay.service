[Unit]
Description=follow the host window size in the guest display
After=firstBoot.service display-manager.service

[Service]
Type=simple
ExecStart=/usr/local/lib/try/tryDisplay

[Install]
WantedBy=graphical.target
